// Add to existing types
export interface Material {
  id: string;
  codigo: string; // New field for product code
  nome: string;
  quantidade: number;
  unidade: string;
  minimo: number;
  projeto: string;
  ultimaAtualizacao: string;
}